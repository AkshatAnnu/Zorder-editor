# Server Configuration
SERVER_URL=http://your-server-or-ngrok-url:8000

# Agent Configuration
MACHINE_ID=COUNTER-1
RECORD_DIR=C:\Recordings
RECORD_SECONDS=180

# Optional: Polling and timing configuration
POLL_INTERVAL=5
ARM_DURATION=600

# Optional: HMAC secret (must match server)
HMAC_SECRET=your_hmac_secret_key_here
